apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "annotation-interface.fullname" . }}-test-connection"
  labels:
    {{- include "annotation-interface.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "annotation-interface.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
